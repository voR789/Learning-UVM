# DUT boundary

UI-10 isolates transaction-observation coverage and therefore uses a
deterministic publisher instead of RTL. A later integration module will sample
transactions reconstructed from a real interface monitor.
