# UB-05 Reflection

Answer briefly after the test passes:

1. What incorrect checker behavior occurs when one meaningful field is omitted
   from copy/compare?
2. Why is a successful `randomize()` call insufficient proof that the generated
   transaction is legal?
3. Which test and seed reproduce your result?
