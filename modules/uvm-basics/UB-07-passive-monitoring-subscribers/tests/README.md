# Checkpoint evidence

The default runner delegates to the passing UI-G1 integration test. Its
scoreboard also rejects the previously verified decrement-as-increment DUT;
UB-07 does not duplicate that fixture.
