# UB-03 Hints

No implementation hints are required. If the concept needs refreshing, trace
one physical interface handle from UI-G1 top-level `set` through component-side
`get` and missing-handle failure policy.
