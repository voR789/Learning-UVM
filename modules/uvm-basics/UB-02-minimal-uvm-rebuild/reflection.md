# UB-02 Reflection

Answer briefly in your own words after the test passes.

1. What would fail if `worker` were created with the wrong parent?
2. Why does the test own the objection instead of the worker?
3. Which output and seed reproduce your completed behavior?
