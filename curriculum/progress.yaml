schema_version: 1
learner:
  weekly_study_hours: null
  vivado_version: "2025.2"
  uvm_version: "1.2"
current_focus: UI-09
last_updated: "2026-07-23"
summary:
  state: guided
  highest_gate_passed: FV-G1
  notes:
    - Repository foundation created before exercise implementation.
    - Foundation micro-modules FV-00 through FV-07 are complete with guided evidence.
    - FV-G1 completed with guided evidence and passed the foundations integration gate.
    - UI-01 completed with guided evidence; UI-02 is the next eligible module and current focus.
    - UI-02 completed with guided evidence; UI-03 is the next eligible module and current focus.
    - UI-03 teach-first package, macro, registration, and compile-order module scaffolded and fixture-verified.
    - UI-03 completed with guided evidence; UI-04 is the next eligible module and current focus.
    - UI-04 code-first component hierarchy module scaffolded and individually fixture-verified in XSim 2025.2.
    - UI-04 completed with guided evidence; UI-05 is the next eligible module and current focus.
    - UI-05 denser active/passive reusable-environment module scaffolded and individually fixture-verified.
    - UI-05 completed with guided evidence; UI-06 is the next eligible module and current focus.
    - UI-06 denser blocking-TLM ports, exports, imp, FIFO, and completion-barrier module scaffolded and fixture-verified.
    - UI-06 completed with guided evidence; UI-07 is the next eligible module and current focus.
    - UI-07 completed with guided evidence; UI-08 is the next eligible module and current focus.
    - UI-08 completed with guided evidence; UI-09 is the next eligible module and current focus.
modules:
  FV-00:
    state: guided
    attempts: 1
    completed_on: "2026-07-13"
    score: 97
    hints_used:
      - Command-line compile, elaborate, and simulation workflow explanation.
      - XSim waveform database versus live snapshot guidance.
      - Correction of elaboration responsibilities and generated log names.
    seeds:
      - 2025
    artifacts:
      - modules/foundations/FV-00-xsim-uvm-smoke/reflection.md
      - modules/foundations/FV-00-xsim-uvm-smoke/tests/verify-runner.ps1
    strengths:
      - Explained why a successful XSim process exit does not prove a UVM test passed.
      - Connected test name and seed to reproducible failures.
      - Demonstrated both the known-pass and deliberate-failure runner paths.
    gaps:
      - Reflection should use the exact generated log names elaborate.log and simulation.log.
    reviewer_notes:
      - Known-pass test returned zero with TEST_RESULT PASS and no UVM errors at seed 2025.
      - Deliberate UVM error returned nonzero and the runner self-test completed successfully.
      - Guided reflects the substantial workflow and waveform coaching used during completion.
  FV-01:
    state: guided
    attempts: 1
    completed_on: "2026-07-14"
    score: 91
    hints_used:
      - Guided review questions for traceability, boundary behavior, and failure criteria.
      - Direct mechanical radix formatting and failure-criterion completion.
      - Scope, assumptions, and completion-criteria wording guidance.
    seeds: []
    artifacts:
      - modules/foundations/FV-01-verification-plan/plan/verification-plan.md
      - modules/foundations/FV-01-verification-plan/reflection.md
    strengths:
      - Traced all eight ALU requirements across 16 directed cases.
      - Distinguished stimulus, observation, expected behavior, and failure criteria.
      - Covered valid operations, invalid selectors, zero behavior, carry, and borrow behavior.
    gaps:
      - Arithmetic boundary selection required coaching and is not yet independently reused.
    reviewer_notes:
      - Module scaffolded after FV-00 evidence made the prerequisite eligible.
      - Structural checker passed with requirements=8 and cases=16 on 2026-07-14.
      - Semantic review confirmed executable expected values and complete output mismatch criteria.
      - Score: traceability 30/30, boundary and negative intent 21/25, observation and failure criteria 24/25, feasibility and clarity 8/10, reflection 8/10.
  FV-02:
    state: guided
    attempts: 1
    completed_on: "2026-07-14"
    score: 96
    hints_used:
      - Combinational settling and simulator scheduling explanation.
      - Review questions for check counting, error accumulation, and four-state comparisons.
      - Mechanical generation of check_case calls from the completed verification plan.
    seeds:
      - 1
    artifacts:
      - modules/foundations/FV-02-directed-alu-testbench/tb/alu_tb.sv
      - modules/foundations/FV-02-directed-alu-testbench/reflection.md
    strengths:
      - Implemented a reusable self-checking task with four-state comparisons and field-level diagnostics.
      - Executed all 16 planned cases with an explicit nonzero settling delay.
      - Demonstrated that an incorrect expected invalid flag produces a counted failure and nonzero runner exit.
    gaps:
      - Case-data transcription was generated directly rather than practiced manually.
    reviewer_notes:
      - Eligible after guided completion of FV-01; starter module created on 2026-07-14.
      - Starter compiled and elaborated in XSim 2025.2, then failed intentionally because zero directed checks executed.
      - Normal XSim 2025.2 regression passed 16 checks at seed 1 on 2026-07-14.
      - Score: functional checking 35/35, separation and reuse 20/20, combinational sampling 15/15, completion and diagnostics 18/20, reflection 8/10.
  FV-03:
    state: guided
    attempts: 1
    completed_on: "2026-07-15"
    score: 90
    hints_used:
      - Class-handle alias prediction and focused experiment guidance.
      - String formatting guidance for sprint using sformatf.
      - Review questions that converted printed demonstrations into self-checking invariants.
    seeds:
      - 1
    artifacts:
      - modules/foundations/FV-03-transaction-models/tb/transaction_lab.sv
      - modules/foundations/FV-03-transaction-models/reflection.md
    strengths:
      - Implemented field-wise copy, compare, and transaction formatting methods.
      - Demonstrated alias behavior and independent-copy behavior with executable checks.
      - Proved a changed copied field does not mutate the original object.
    gaps:
      - Reflection wording still partially conflates class-handle semantics with object-oriented programming generally.
      - Copying the diagnostic name makes original and copy logs less distinguishable.
    reviewer_notes:
      - Eligible after guided completion of FV-02; starter module created on 2026-07-14.
      - Starter compiled and elaborated in XSim 2025.2, then failed intentionally because no transaction checks executed.
      - Final XSim 2025.2 transaction lab passed at seed 1 on 2026-07-15.
      - Score: representation 20/20, handle reasoning 22/25, copy and compare 28/30, diagnostics 14/15, reflection 6/10.
  FV-04:
    state: guided
    attempts: 1
    completed_on: "2026-07-15"
    score: 94
    hints_used:
      - Randomize return-value and failure-policy explanation.
      - Guidance to widen arithmetic inside overflow constraints.
      - Seed-reproducibility review and reflection refinement.
    seeds:
      - 1
      - 2
    artifacts:
      - modules/foundations/FV-04-constrained-random/tb/randomization_lab.sv
      - modules/foundations/FV-04-constrained-random/reflection.md
    strengths:
      - Generated and validated 20 legal constrained-random transactions.
      - Used a targeted inline constraint for ADD overflow with widened arithmetic.
      - Correctly handled an intentionally unsatisfiable constraint.
      - Demonstrated identical sequences for repeated seed 1 and a different sequence for seed 2.
    gaps:
      - Reusable-constraint reflection wording remains broader than the implemented per-item legality invariant.
    reviewer_notes:
      - Eligible after guided completion of FV-03; starter module created on 2026-07-15.
      - Starter compiled and elaborated in XSim 2025.2, then failed intentionally because no randomization checks executed.
      - Final XSim 2025.2 run passed at seed 1 on 2026-07-15; seed 2 also passed with a different sequence.
      - Score: legal generation 25/25, constraint design 24/25, failure handling 20/20, reproducibility 18/20, reflection 7/10.
  FV-05:
    state: guided
    attempts: 1
    completed_on: "2026-07-15"
    score: 86
    hints_used:
      - Modport perspective and syntax explanation.
      - Correction from hierarchical modport access to direct virtual-interface signal access.
      - Reflection guidance on static interfaces and dynamic class handles.
    seeds:
      - 1
    artifacts:
      - modules/foundations/FV-05-systemverilog-interface/tb/alu_if.sv
      - modules/foundations/FV-05-systemverilog-interface/tb/interface_lab.sv
      - modules/foundations/FV-05-systemverilog-interface/reflection.md
    strengths:
      - Declared correct DUT and testbench modport directions.
      - Connected a class-based checker to the concrete interface and performed self-checking drive/wait/sample behavior.
      - Final XSim run passed without illegal-modport warnings.
    gaps:
      - The class stores generic virtual alu_if rather than virtual alu_if.tb_mp, so compile-time modport restrictions are not retained.
      - Reflection describes the modport-limited handle more precisely than the final declaration implements it.
    reviewer_notes:
      - Eligible after guided completion of FV-04; starter module created on 2026-07-15.
      - Starter compiled and elaborated in XSim 2025.2, then failed intentionally because no interface checks executed.
      - Final XSim 2025.2 interface run passed at seed 1 on 2026-07-15.
      - Learner reported deliberate expected-value fault testing before restoring the passing case.
      - Score: interface and modports 25/25, virtual-interface use 17/25, timing and checking 30/30, fault evidence 8/10, reflection 6/10.
  FV-06:
    state: guided
    attempts: 1
    completed_on: "2026-07-16"
    score: 92
    hints_used:
      - Blocking mailbox and fresh-object ownership guidance.
      - Monitor sampling guidance for registered outputs.
      - End-of-test timeout architecture correction and publication-order invariant coaching.
    seeds:
      - 1
    artifacts:
      - modules/foundations/FV-06-concurrent-synchronization/tb/concurrency_lab.sv
      - modules/foundations/FV-06-concurrent-synchronization/reflection.md
    strengths:
      - Separated generator, driver, passive monitor, and scoreboard responsibilities.
      - Used blocking mailboxes and events to synchronize reset, transaction flow, and completion.
      - Checked all ten transactions exactly once and prevented premature successful termination.
      - Restored a passing run after demonstrating a deliberate mismatch failure.
    gaps:
      - Required substantial guidance on sampling edges, timeout concurrency, and cross-process publication ordering.
    reviewer_notes:
      - Eligible after guided completion of FV-04 and FV-05; starter module created on 2026-07-15.
      - Starter compiled and elaborated with classes, mailboxes, events, interface, and concurrent processes in XSim 2025.2, then reached the intended 1 us timeout with zero checks.
      - Final XSim 2025.2 run passed ten generated, driven, observed, and checked transactions at seed 1 on 2026-07-16.
      - Learner reported deliberate fault testing before restoring the expected sum.
      - Score: concurrent architecture 20/20, synchronization and reset 22/25, transaction communication 19/20, checking and termination 24/25, reflection 7/10.
  FV-07:
    state: guided
    attempts: 1
    completed_on: "2026-07-16"
    score: 93
    hints_used:
      - Coverage-versus-correctness invariant and focused reflection questions.
      - XSim functional-coverage database and report workflow guidance.
      - Direct repair of per-encoding operation bins, the defined-operation-by-zero cross, and closure stimulus.
    seeds:
      - 1
    artifacts:
      - modules/foundations/FV-07-functional-coverage/tb/coverage_lab.sv
      - modules/foundations/FV-07-functional-coverage/reflection.md
    strengths:
      - Defined operand-class and result-zero coverage and sampled once per completed transaction.
      - Used the XSim report to identify missing zero operand bins and added purposeful stimulus.
      - Explained that coverage measures occurrence rather than DUT correctness.
      - Located transaction-based coverage after passive observation of a completed transaction.
    gaps:
      - Initially grouped operation encodings too coarsely for requirement-level hole detection.
      - Required direct repair of the cross model and remaining closure stimulus.
    reviewer_notes:
      - Eligible after guided completion of FV-04; starter module created on 2026-07-16.
      - XSim 2025.2 compatibility probe passed covergroup sampling and get_inst_coverage at 100 percent.
      - Starter compiled, elaborated, sampled six transactions, and failed intentionally at 0 percent because the learner coverage model is empty.
      - Final XSim 2025.2 run passed at seed 1 with 13 samples, eight operation bins, complete operand and result-zero bins, and all ten reachable operation-by-zero cross bins covered.
      - Score: requirement-derived model 30/30, cross coverage 20/20, sampling correctness 20/20, coverage analysis and closure 15/20, reflection 8/10.
      - Guided reflects the direct repair used to restore per-encoding observability and valid cross closure.
  FV-G1:
    state: guided
    attempts: 1
    completed_on: "2026-07-22"
    score: 89
    hints_used:
      - Requirement-level traceability, scenario bundling, and coverage-intent coaching.
      - Independent-model acceptance, passive-observation timing, and failure-criterion guidance.
      - Direct completion of simultaneous-operation, full-recovery, and wraparound plan rows.
      - Non-UVM transaction, mailbox termination, predictor, scoreboard, assertion, and coverage architecture guidance.
    seeds:
      - 1
      - 231
      - 353
    artifacts:
      - modules/foundations/FV-G1-fifo-integration/spec/fifo-hardware-specification.md
      - modules/foundations/FV-G1-fifo-integration/plan/verification-plan.md
      - modules/foundations/FV-G1-fifo-integration/implementation-handoff.md
      - modules/foundations/FV-G1-fifo-integration/tb/fifo_tb.sv
    strengths:
      - Distinguished request intent, passive observation, independent prediction, and DUT output checking.
      - Developed reset, full/empty boundary, rejected-read recovery, and simultaneous-operation scenarios.
      - Used later observable operations to expose hidden pointer or ordering corruption.
      - Completed requirement traceability across all 34 normative FIFO requirements.
      - Implemented an independent queue-based predictor, passive monitor, field-wise scoreboard, assertions, deterministic termination, and requirement-derived coverage.
      - Localized the seeded full-boundary defect and restored passing behavior with the same reproducing seed.
    gaps:
      - Required substantial coaching on edge-relative timing, acceptance semantics, and concise failure criteria.
      - Final planning rows were completed with direct assistance.
      - Required substantial coaching on end-marker flow, concurrency, coverage lifetime, assertion placement, and completion invariants.
      - UVM architecture mapping remains intentionally broad before the UVM Introduction phase.
    reviewer_notes:
      - Eligible after guided completion of FV-06 and FV-07.
      - Verification plan reached 34 of 34 requirement IDs with zero unknown IDs on 2026-07-18.
      - XSim 2025.2 passed seeds 1, 231, and 353 on 2026-07-22 with 94 scoreboard checks, drained end-marker flow, 100 percent functional coverage, explicit TEST_RESULT PASS, and exit code zero for each run.
      - Fault evidence records the first full mismatch at 146000 ps under seed 1 and identifies FIFO-STATE-003 as the governing requirement.
      - Score: functional correctness and fault detection 34/35, architecture and separation 18/20, protocol/timing/reset/concurrency 13/15, assertions and coverage 9/10, reproducibility/diagnostics/evidence 8/10, explanation/reflection 7/10.
      - Guided reflects substantial direct architecture and debugging assistance; later independent reuse is unproven.
  UI-01:
    state: guided
    attempts: 1
    completed_on: "2026-07-22"
    score: 94
    hints_used:
      - Teach-first explanation of UVM objects, persistent components, transaction flow, and lifecycle.
      - Sequence-item, sequence, sequencer, and driver responsibility clarification.
      - Predictor-versus-scoreboard independence and coverage-subscriber ownership coaching.
      - Run-phase objection and component-completion distinction.
    seeds:
      - 1
    artifacts:
      - modules/uvm-introduction/UI-01-map-procedural-uvm/exercise/architecture-map.md
      - modules/uvm-introduction/UI-01-map-procedural-uvm/reflection.md
    strengths:
      - Mapped all seven procedural verification responsibilities to plausible UVM roles.
      - Distinguished short-lived sequence items and sequences from persistent UVM components.
      - Preserved active driving, passive observation, independent prediction, checking, and coverage boundaries.
      - Explained that UVM standardizes structure while DUT-specific prediction, stimulus intent, and coverage still come from the specification.
    gaps:
      - UVM class construction, inheritance, factory registration, and exact lifecycle rules remain unfamiliar by design.
      - Initially conflated sequence items with pin interfaces and predictors with correctness checking.
    reviewer_notes:
      - Eligible after guided completion of FV-G1.
      - Learner requested a teach-first introduction with reading and a worked example before implementation.
      - Module scaffolded on 2026-07-22; XSim 2025.2 hierarchy example and valid mapping fixture passed at seed 1, while the starter and invalid mapping fixture failed as intended.
      - Learner architecture map passed semantic review and MAP_RESULT validation with seven rows and two predictions.
      - Final XSim 2025.2 hierarchy run passed at seed 1 with zero UVM errors and zero UVM fatals.
      - Score: responsibility mapping 35/35, object/component reasoning 18/20, transaction flow 20/20, executable observation 10/10, reuse reasoning 5/5, reflection 6/10.
      - Guided reflects teach-first instruction and multiple conceptual corrections; later independent reuse is unproven.
  UI-02:
    state: guided
    attempts: 1
    completed_on: "2026-07-22"
    score: 94
    hints_used:
      - Teach-first explanation of base classes, derived classes, overriding, handles, and virtual dispatch.
      - Base-handle versus base-object terminology correction.
      - Non-virtual dispatch prediction and four-state equality review.
    seeds:
      - 1
    artifacts:
      - modules/uvm-introduction/UI-02-inheritance-polymorphism/tb/policy_lab.sv
      - modules/uvm-introduction/UI-02-inheritance-polymorphism/reflection.md
    strengths:
      - Implemented exact and tolerance policies with correct behavior across six required checks.
      - Used one unchanged base-handle caller to dispatch to both derived policy overrides.
      - Corrected exact comparison so matching unknown values are not silently accepted.
      - Explained how virtual dispatch permits policy replacement without caller type branching.
    gaps:
      - Reflection wording initially described a base handle as a base object and a derived method as impossible to override when non-virtual.
      - UVM-specific construction, registration, and package syntax remain intentionally deferred to UI-03.
    reviewer_notes:
      - Eligible after guided completion of UI-01.
      - Teach-first module scaffolded on 2026-07-22 to address class construction, inheritance, handles, overriding, and virtual dispatch.
      - XSim 2025.2 valid polymorphism fixture passed six checks at seed 1.
      - Non-virtual base-method fixture failed as intended on tolerance-policy dispatch through a base handle.
      - Learner starter compiled and elaborated, then failed intentionally with three policy mismatches because its TODO methods return false.
      - Final learner implementation passed all six checks at seed 1 with explicit TEST_RESULT PASS.
      - Score: policy behavior 30/30, polymorphic dispatch 25/25, inheritance and handle reasoning 17/20, fault detection and diagnostics 10/10, reproducibility and clarity 5/5, reflection 7/10.
      - Guided reflects teach-first instruction and conceptual corrections; later independent reuse is unproven.
  UI-03:
    state: guided
    attempts: 1
    completed_on: "2026-07-22"
    score: 92
    hints_used:
      - Teach-first explanation of packages, namespaces, imports, textual includes, macros, registration, and factory construction.
      - Factory registration, type_id, construction, and override mental-model coaching.
      - Import-versus-namespace and include-location corrections.
      - Factory override versus config_db distinction.
    seeds:
      - 1
    artifacts:
      - modules/uvm-introduction/UI-03-packages-macros/tb/ui03_pkg.sv
      - modules/uvm-introduction/UI-03-packages-macros/exercise/syntax-map.md
      - modules/uvm-introduction/UI-03-packages-macros/reflection.md
    strengths:
      - Registered ui03_packet and constructed it through type_id without changing the caller.
      - Distinguished compiled package declarations from textually included macro definitions.
      - Explained why dependency order precedes import visibility.
      - Connected factory construction to compatible type replacement through factory overrides.
    gaps:
      - Reflection omitted an explicit statement that registration alone creates no instance, although this was demonstrated correctly during practice.
      - Some worksheet wording remains informal around include placement and returned object handles.
    reviewer_notes:
      - Eligible after guided completion of UI-02.
      - Teach-first module scaffolded on 2026-07-22 for packages, imports, includes, compile order, UVM object registration, and type_id construction.
      - XSim 2025.2 valid registered-object fixture passed at seed 1 with the required type, name, and value.
      - Reversed compilation order failed as intended because the consumer could not resolve ui03_pkg.
      - Learner starter failed as intended because ui03_packet lacks the registration macro and therefore type_id.
      - Final learner run passed at seed 1 with type ui03_packet, name packet, value 42, and MAP_RESULT-equivalent syntax validation for five rows and one prediction.
      - Score: package organization and compile order 24/25, imports and includes 18/20, UVM object registration 23/25, construction evidence 10/10, diagnostic reasoning 9/10, reflection 8/10.
      - Guided reflects extensive teach-first explanation and multiple conceptual corrections; later independent reuse is unproven.
  UI-04:
    state: guided
    attempts: 1
    completed_on: "2026-07-23"
    score: 95
    hints_used:
      - Teach-first explanation of constructors versus build_phase and top-down UVM component construction.
      - uvm_phase type, phase handle, placeholder child_type, and runtime parent-path explanation.
      - Wrong-parent fixture interpretation and persistent-component lifecycle correction.
      - Handle declaration versus factory-created instance return-value correction.
    seeds:
      - 1
    artifacts:
      - modules/uvm-introduction/UI-04-component-hierarchy/tb/ui04_pkg.sv
      - modules/uvm-introduction/UI-04-component-hierarchy/reflection.md
    strengths:
      - Factory-created the container and leaf during their owners' build phases with correct names and parents.
      - Produced and explained the exact uvm_test_top.container.leaf hierarchy.
      - Distinguished static HDL top, dynamic UVM test top, runtime instance names, parents, and full paths.
      - Explained that objections prevent phase termination but do not prove work is complete or correct.
    gaps:
      - Initially generalized build_phase to all objects rather than persistent component structure.
      - Needed clarification between a declared handle variable and the handle value returned for a factory-created instance.
    reviewer_notes:
      - Eligible after guided completion of UI-03.
      - Code-first module scaffolded on 2026-07-22 for run_test, registered tests, build_phase, component parenting, topology, run_phase, and objections.
      - XSim 2025.2 valid fixture passed at seed 1 with the required uvm_test_top.container.leaf path.
      - Wrong-parent fixture failed as intended after placing leaf at uvm_test_top.leaf.
      - Learner starter compiled and elaborated, then failed as intended because no container was created.
      - Final learner hierarchy passed at seed 1 with zero UVM errors/fatals and explicit TEST_RESULT PASS.
      - Score: component hierarchy 30/30, factory construction and parenting 25/25, lifecycle reasoning 18/20, negative-case detection 10/10, reproducibility and diagnostics 5/5, reflection 7/10.
      - Guided reflects extensive teach-first lifecycle and terminology coaching; later independent reuse is unproven.
  UI-05:
    state: guided
    attempts: 1
    completed_on: "2026-07-23"
    score: 95
    hints_used:
      - Active/passive runner selection and configuration-flow explanation.
      - Inherited requested_active field and constructor-chain clarification.
      - Constructor invocation versus build-phase callback explanation.
      - Passive-driver contention reflection prompt.
      - Direct repair of an XSim kernel crash caused by a maybe-null method call inside a ternary diagnostic.
    seeds:
      - 1
    artifacts:
      - modules/uvm-introduction/UI-05-reusable-environment/tb/ui05_pkg.sv
      - modules/uvm-introduction/UI-05-reusable-environment/reflection.md
      - modules/uvm-introduction/UI-05-reusable-environment/implementation-handoff.md
    strengths:
      - Composed one environment containing agent, predictor, and scoreboard roles.
      - Built an always-present monitor and conditionally present driver from propagated configuration.
      - Passed both active and passive tests with the required distinct topologies.
      - Explained agent, environment, test, configuration, and future transaction-connection boundaries.
    gaps:
      - Needed clarification on inherited fields, constructor timing, and the difference between object construction and build-phase callbacks.
      - Reflection grammar and exact configuration-flow terminology required direct editing.
    reviewer_notes:
      - Eligible after guided completion of UI-04.
      - Denser code-first module scaffolded on 2026-07-23 for agent/environment/test/config ownership and active/passive structural reuse.
      - XSim 2025.2 valid active and passive configurations each passed at seed 1 with their required topologies.
      - Always-driver fixture failed passive mode as intended with one UVM fatal.
      - Learner starter compiled and elaborated, then failed because env children were not yet built.
      - Final learner active and passive runs each passed at seed 1 with zero UVM errors/fatals.
      - Passive mode required a scaffold compatibility repair after XSim crashed on a null-handle method call in an unselected ternary branch; explicit if/else guarding restored the pass.
      - Score: reusable environment composition 30/30, active/passive policy 25/25, configuration ownership 18/20, negative-case detection 10/10, diagnostics and reproducibility 5/5, reflection 7/10.
      - Guided reflects lifecycle/configuration coaching and direct grammar plus XSim compatibility repair; later independent reuse is unproven.
  UI-06:
    state: guided
    attempts: 1
    completed_on: "2026-07-23"
    score: 92
    hints_used:
      - Expanded first-principles TLM explanation covering ports, exports, imps, FIFO buffering, call direction, and data direction.
      - Direct-push put versus buffered-pull get explanation.
      - Barrier threshold, participant arrival, and timeout behavior explanation.
      - Concrete receiver-class coupling versus transaction-type coupling correction.
    seeds:
      - 1
    artifacts:
      - modules/uvm-introduction/UI-06-tlm-connections/tb/ui06_pkg.sv
      - modules/uvm-introduction/UI-06-tlm-connections/reflection.md
      - modules/uvm-introduction/UI-06-tlm-connections/reading/tlm-mental-model.md
    strengths:
      - Correctly connected producer and consumer through FIFO put/get exports.
      - Correctly connected a producer put port to a terminal audit imp.
      - Produced exactly one consumer check, one audit check, and a drained FIFO.
      - Explained TLM as typed endpoint wiring whose routing is owned by the environment.
    gaps:
      - Required extensive teaching to distinguish ports, exports, imps, push versus pull, and method-call versus data direction.
      - Barrier behavior was initially omitted from instruction and required direct explanation.
      - Reflection terminology still loosely calls concrete receiver APIs transaction methods.
    reviewer_notes:
      - Eligible after guided completion of UI-05.
      - Denser code-first module scaffolded on 2026-07-23 for typed blocking ports, FIFO exports, terminal imps, connect_phase, and deterministic barrier completion.
      - XSim 2025.2 valid fixture passed at seed 1 with one consumer check, one audit check, and fifo_used=0.
      - Misroute fixture failed at timeout because the audit destination never received the item or reached the barrier.
      - Learner starter failed at time zero with three unconnected-port errors and a UVM build-error fatal.
      - Final learner run passed at seed 1 with consumer_checks=1, audit_checks=1, fifo_used=0, and zero UVM errors/fatals.
      - Score: FIFO transaction path 25/25, direct implementation path 20/20, endpoint and lifecycle reasoning 16/20, completion and fault detection 18/20, diagnostics and reproducibility 5/5, reflection 8/10.
      - Guided reflects extensive first-principles TLM and synchronization coaching; later independent reuse is unproven.
  UI-07:
    state: guided
    attempts: 1
    completed_on: "2026-07-23"
    score: 96
    hints_used:
      - Teach-first explanation of sequence, sequencer, driver roles and the four-call request handshake.
      - Clarification that start_item obtains a grant rather than beginning object construction.
      - Clarification that finish_item waits for the driver completion acknowledgment.
      - Reflection coaching distinguishing sequencer arbitration from FIFO behavior.
    seeds:
      - 1
    artifacts:
      - modules/uvm-introduction/UI-07-transaction-stimulus/tb/ui07_pkg.sv
      - modules/uvm-introduction/UI-07-transaction-stimulus/reading/sequence-handshake.md
      - modules/uvm-introduction/UI-07-transaction-stimulus/worked-example.md
    strengths:
      - Implemented the explicit start_item and finish_item sequence-side handshake.
      - Implemented the get_next_item and item_done driver-side handshake.
      - Connected driver.seq_item_port to sequencer.seq_item_export in connect_phase.
      - Completed and checked three deterministic requests in order with zero UVM errors or fatals.
    gaps:
      - Initially described start_item as the beginning of object construction rather than the arbitration grant boundary.
      - Used FIFO terminology for sequencer arbitration in a source comment.
      - Required guided reflection revisions to distinguish request submission from completion acknowledgment.
    reviewer_notes:
      - Eligible after guided completion of UI-06.
      - Scaffolded on 2026-07-23 as an executable bridge from general TLM recognition to the sequence-item handshake.
      - Valid fixture passed at seed 1 with exactly three completed items and zero UVM errors/fatals.
      - Missing-item_done fixture failed at the 1 us timeout as intended.
      - Untouched learner starter compiled and elaborated, then failed on its absent request handshake as intended.
      - Final learner run passed at seed 1 with completed=3 and zero UVM errors/fatals.
      - Score: sequence handshake and data 25/25, driver handshake and checking 30/30, TLM connection and reasoning 18/20, fault detection and completion 15/15, reflection 8/10.
      - Guided reflects teach-first instruction and multiple mental-model corrections; later independent reuse is unproven.
  UI-08:
    state: guided
    attempts: 1
    completed_on: "2026-07-23"
    score: 96
    hints_used:
      - Teach-first explanation of analysis broadcast, subscriber exports, and analysis implementations.
      - Clarification that an analysis implementation endpoint and its write method form one receive path.
      - Subscriber-versus-analysis-implementation architecture comparison.
      - Reflection corrections for the missing audit connection and nonblocking analysis semantics.
    seeds:
      - 1
    artifacts:
      - modules/uvm-introduction/UI-08-analysis-layer/tb/ui08_pkg.sv
      - modules/uvm-introduction/UI-08-analysis-layer/reflection.md
      - modules/uvm-introduction/UI-08-analysis-layer/reading/analysis-mental-model.md
    strengths:
      - Declared, constructed, and used one typed analysis port.
      - Connected one publisher to a subscriber export and an independent analysis implementation.
      - Demonstrated deterministic one-to-many broadcast with both consumers checking all three observations.
      - Distinguished a dedicated subscriber from a flexible analysis implementation endpoint.
      - Explained that analysis write has no request or completion handshake.
    gaps:
      - Initial reflection confused the missing audit branch with the subscriber branch.
      - Initially described analysis broadcast as global and as disregarding handshake rules.
      - Exercise implementation density was too low for the learner's desired practice level.
    reviewer_notes:
      - Eligible after guided completion of UI-07.
      - Valid fixture passed at seed 1 with published/subscriber/audit counts of 3/3/3.
      - Missing-audit fixture failed at the independent count oracle with 3/3/0.
      - Final learner run passed XSim 2025.2 at seed 1 with zero UVM errors/fatals.
      - Score: publication and fault detection 35/35, architecture 20/20, connection semantics 20/20, reproducibility 10/10, request-versus-analysis reasoning 4/5, reflection 7/10.
      - Guided reflects teach-first instruction and multiple conceptual corrections; later independent reuse is unproven.
evidence_schema:
  state: not_started
  attempts: 0
  completed_on: null
  score: null
  hints_used: []
  seeds: []
  artifacts: []
  strengths: []
  gaps: []
  reviewer_notes: []
